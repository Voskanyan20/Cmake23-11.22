#include <iostream>
#include <string>
#include "functions.h"
using namespace std;
int main() {
    char s;

    int* arr bigNumb(s);
    cout << "write your number" << endl;

    cin >> s;

    for (int i = 0; i < s.size(); i++) {
        cout << arr[i];
    cout << bigNumb(factor(arr[i])) << endl;
    }


    return 0;
}
